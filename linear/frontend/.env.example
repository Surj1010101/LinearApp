# Frontend configuration
# Copy to ".env" (same folder) for local dev, or set these in Vercel project settings.

# Base URL of the backend API. Include /api at the end.
# Local dev default:  http://localhost:8000/api
# Production:         https://<your-render-service>.onrender.com/api
VITE_API_URL=http://localhost:8000/api
